<!DOCTYPE html>
<html>
  <head>
    <title><%= yield :subtitle %> - Three Twins Productions, Inc - Screen Printing and Embroidery</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description"
          content="Three Twins Productions provides Screen Printing and Embroidery services in Watertown, MA." />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= favicon_link_tag "favicon.ico" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="<%= yield(:bg_navy) %>">
    <%= render partial: "layouts/shared/main_nav" %>

    <% if notice.present? %>
      <div class="notice"><%= notice %></div>
    <% end %>

    <% if alert.present? %>
      <div class="alert"><%= alert %></div>
    <% end %>
    
    <%= content_for?(:content) ? yield(:content) : yield %>

    <%= render partial: "layouts/shared/footer" %>
    <%= render partial: "layouts/shared/modal_app" %>
  </body>
</html>
